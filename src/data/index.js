import dataColours from './colours'
import dataNumbers from './numbers'

const initialState = { colour: {colours: dataColours, active: 'red'},
                       number: {numbers: dataNumbers, active: 'two'}
                     }
export default initialState
